#pragma once

// Automatic DLL acquisition is disabled: the project provides no trusted
// publisher signature or independently pinned artifact digest.
// DLLs must be selected explicitly in the UI. No update server is contacted.
